spring:
  datasource:
    username: mc
    password: Chudeptrai123
    url: jdbc:postgresql://192.168.1.99:15432/catalog-service
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
      # Hibernate ddl auto (create, create-drop, validate, update)
    show-sql: true
logging:
  level:
    root: info

polar:
  greeting: Welcome to the local book catalog!
---
spring:
  config:
    activate:
      on-profile: test
